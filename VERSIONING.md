# 版本管理规则

## 当前版本基线

重构后的项目从 `1.0.0` 重新开始版本管理。原型的 `4.2.0` 仅是历史继承值，不代表当前架构已经经历四个正式主版本。

在创建正式 GitHub Release 前，`manifest.json` 是扩展版本号的唯一事实来源。正式发布时，Git Tag、GitHub Release 标题和 `manifest.json` 必须保持一致。

## 版本格式

项目采用 `主版本.次版本.修订版本`：

- 修订版本，例如 `1.0.1`：缺陷修复、官网选择器兼容和不改变现有行为的可靠性优化。
- 次版本，例如 `1.1.0`：新增向后兼容的功能、设置或数据字段。
- 主版本，例如 `2.0.0`：存在破坏性的数据格式、交互方式、权限或兼容策略变化。

## 发布流程

1. 确认工作区只包含计划发布的产品代码和文档。
2. 更新 `manifest.json` 和 `CHANGELOG.md`，将“待发布”替换为发布日期。
3. 完成脚本语法检查、本地回归测试和 Chrome 实页验收。
4. 创建发布提交。
5. 使用与清单一致的版本创建 Git Tag，例如 `v1.0.0`。
6. 创建 GitHub Release，并附上可直接加载的扩展压缩包和升级说明。

Git Tag 只在正式发布包确认后创建，不为普通开发提交提前打标签。
